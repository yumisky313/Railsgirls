<h1><%= @board.title %></h1>

<table>
<% @posts.each do |post| %>
	<tr>
		<td>
			<%= post.author %>
		</td>
		<td>
			<%= post.content %>
		</td>
	</tr>
	<% end %>
</table>

<%= link_to "Back", boards_path %>