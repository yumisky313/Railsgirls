<h1><%= @board.title %></h1>

<table>
<% @posts.each do |post| %>
	<tr>
		<td>
			<%= link_to post.author, board_post_path(@board, post) %>
		</td>
	</tr>
	<% end %>
</table>

<%= link_to "Edit", edit_board_path(@board) %>
<%= link_to "Back", boards_path %>