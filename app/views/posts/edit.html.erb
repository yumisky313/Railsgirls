<%= form_for(@post, :url => board_post_path(@board, @post)) do |f| %>
<%= render :partial => 'form', :locals => {:f => f} %>
<%= f.submit "Save" %>
<% end %>
<%= link_to "Back", board_post_path(@board,@post) %>